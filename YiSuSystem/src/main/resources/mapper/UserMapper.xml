<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yisusystem.mapper.UserMapper">

        <resultMap id="UserResultMap" type="com.yisusystem.pojo.entity.User">
                <id column="user_id" property="userId" />
                <result column="user_name" property="userName" />
                <result column="password" property="password" />
                <result column="salt" property="salt" />
                <result column="phone" property="phone" />
                <result column="real_name" property="realName" />
                <result column="avatar" property="avatar" />
                <result column="email" property="email" />
                <result column="role" property="role" />
                <result column="create_time" property="createTime" />
                <result column="update_time" property="updateTime" />
        </resultMap>

        <!-- 根据多个 id 批量查询用户 -->
        <select id="getUserByIds" resultMap="UserResultMap" parameterType="java.util.List">
                SELECT
                        user_id,
                        user_name,
                        password,
                        salt,
                        phone,
                        real_name,
                        avatar,
                        email,
                        role,
                        create_time,
                        update_time
                FROM `user`
                WHERE user_id IN
                <foreach collection="list" item="id" open="(" separator="," close=")">
                        #{id}
                </foreach>
        </select>

        <!-- 根据用户名查询用户 -->
        <select id="getUserByUsername" resultMap="UserResultMap" parameterType="String">
                SELECT
                        user_id,
                        user_name,
                        password,
                        salt,
                        phone,
                        real_name,
                        avatar,
                        email,
                        role,
                        create_time,
                        update_time
                FROM `user`
                WHERE user_name = #{userName}
                LIMIT 1
        </select>

</mapper>
